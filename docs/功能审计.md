# 候票台 0.2 功能与边界审计

核查日期：2026-09-14。范围：本地 Windows 应用、内置网页工作区、Android USB 辅助。以下“已验证”只表示功能测试通过，不代表任何平台实战抢票成功率。

| 能力 | 当前状态 | 验证或边界 |
| --- | --- | --- |
| 固定场次、人数、预算、A/B/C 票档与来源 | 已实现，测试通过 | 表单、规则单测、SQLite 重启验证；可接受票档最多 8 个。来源由用户填写，软件不能独立证明公告真实性。 |
| 开售、优先购、候补、补票、邀请日历 | 已实现，测试通过 | 支持当地时区与截止时间；夏令时无效或歧义时间拒绝保存。没有自动同步主办方改期，需要手动更新。 |
| 运行期间的桌面提醒 | 已实现，逻辑测试通过 | 开售前 24 小时、30 分钟、5 分钟及开始时的固定窗口；关闭应用、系统休眠、通知被系统关闭时不能保证提醒。官方 App 原生通知应保留。 |
| 资格、实名、支付、设备准备清单 | 已实现，UI 测试通过 | 勾选属于用户人工确认，不会读取平台账号或证件。 |
| 人工票档决策 | 已实现，单测与 UI 测试通过 | 保留固定人数和预算；未知状态不会误判售罄；参考单价可能不含费，最终总额必须在官方订单页人工核对。 |
| 参与、排队、待支付、已付款待出票、已出票记录 | 已实现，UI 测试通过 | 付款和出票阶段要求人工填写官方确认依据；不会把点击、可选页面或待支付误报为出票。 |
| 内置官方网页 + 侧边购票条件 | 工作区与会话保持测试通过，真实平台待验证 | 使用独立 `WebContentsView`；不注入脚本、不自动刷新。重复打开同一任务保持同一网页实例。真实官方页面显示和结账需按平台实测。 |
| 平台独立登录会话及清除 | 已实现，UI 测试通过 | Cookie/缓存保存在本机；正在打开的该平台会话禁止清除。清除后重新登录。 |
| Android USB 检测与唤起大麦 | 已实现，待真机验证 | 本机没有 adb/已授权手机，不能宣称已在真机通过；包名和安装渠道可能不同。 |
| 实时手机投屏 | 未实现 | 当前用户提出的“投屏或者打开网页”由内置网页方案满足。App 专属场次仍需看手机原生界面。 |
| 实时库存、自动选票/提交、排队优势 | 未实现 | 未取得平台授权接口；大麦首版按既定方案只做官方流程辅助。不能把平台图标或网页加载等同于自动抢票适配。 |
| 真实成功率提升 | 未测得 | 没有可比的真实开售样本或对照组。只能报告减少准备遗漏、保持会话及人工操作效率的验证结果。 |

## 已核对的关键情况

- **App 专属下单**：内置网页可能只能展示项目；使用官方 App 完成购买，不能从网页加载成功推断可下单。
- **排队与等待室**：同一任务二次打开只聚焦原窗口；不会主动刷新或开多个本地队列。关闭窗口、清理数据或切换官方渠道可能使排位丢失，由用户决定。
- **跨站登录和付款**：网页可能跳转第三方 HTTPS 域名，侧栏会提示域名离开已验证平台；跨站弹窗被阻止时使用系统浏览器或原生 App。系统浏览器的 Cookie 与内置网页不共享，不能保证原队列迁移。
- **网络、系统休眠及平台阻挡**：加载失败会显示错误和原入口回退。网络断开、验证码、机器人检测、区域限制、平台禁止嵌入均由官方页面处理；本工具不绕过。
- **多渠道和官方候补**：机会需要逐场人工录入，不能假定相同演出在多个页面有独立票池；DICE 等候补的票型/人数选择不得照搬普通选票的 A→B→C 决策。
- **可买、锁票、付款、出票**：各阶段分别记录；预算包含人数乘单价的初筛，服务费、税费、配送费与实际总额须人工核对。已有待支付或已确认订单时，票档建议停止，避免重复提交。
- **时间变更与后续机会**：每条机会保留来源与核实日期；官方更改时间需手动修订。首轮结束不会推断“整点回流”；只跟进有来源的补票、候补或邀请。
- **转售和景点**：转售价格与官方首发分开判断；景点付款完成可能仍需预约或供应商确认，记录的“已出票”只能在凭证生效后使用。
- **隐私与本地数据**：任务 SQLite 与内置网页 Cookie/缓存均留在本机；不集中存放朋友的账号、证件或支付信息。卸载前可备份任务库，清除网页数据会退出对应平台登录。

## 仍需真实设备或活动验证

在获准的真实购票场景中，按各平台与活动分别核对官方入口、原生 App 要求、登录/支付跳转、队列保持、候补通知、时区变更、实际总价和订单状态。先比较准备遗漏与完成有效订单所需时间；没有对照结果时不声称提高成功率。Android 真机连接、USB 唤起和任何未来投屏功能也必须在设备上单独验收。
